apiVersion: v1
kind: Pod
metadata:
  namespace: fluxtestapp
  creationTimestamp: null
  labels:
    run: testpod
  name: testpod
spec:
  containers:
  - image: nginx
    name: testpod
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
